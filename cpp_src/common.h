#include <string>
#include <iostream>
#include <fstream>
#include <json/value.h>

using namespace std; // use all std object names etc. 


int LoadJson(string a)
{   std:: cout << a << std::endl;
    return 0;
}